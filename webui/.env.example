# Optional while running the Vite development server with a separate local API.
# The production WebUI expects the API on the same 127.0.0.1 origin by default.
VITE_MAP_API_ORIGIN=http://127.0.0.1:8787
